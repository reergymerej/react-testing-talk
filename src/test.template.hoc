import React from 'react'
import { shallow } from 'enzyme'
import FooContainer from './FooContainer'

const defaultProps = {}

const factory = (props = {}) => (
  <FooContainer.WrappedComponent {...defaultProps} {...props} />
)

describe('<FooContainer />', () => {
  it('should render', () => {
    shallow(factory())
  })
})
